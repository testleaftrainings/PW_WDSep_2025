var age = "30"; // JS is dynamic typing
//Other programming language int age = 30 which means age can hold only number
var phNumber = "908089"; //explicit inference --> TS static typing
console.log("My Phone Number ".concat(phNumber));
